import React from 'react'
import DeckList from './DeckList';

export default async function DeckListPage() {
  return (
    <div>
      {/* @ts-ignore */}
      {/* <DeckList/> */}
      This directory is currently disabled until a stable release of WaniKani (v1.0.0).
    </div>
  )
}
